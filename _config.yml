version: "2.0"

compress_html:
  clippings: all
  comments: ["<!-- ", " -->"]
  startings: [html, head, body]
  endings: [html, head, body]

#build
source: src
destination: _site
sass:
  sass_dir: _sass
  style: compressed
markdown: kramdown
